name: CI
on: [push, workflow_dispatch]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: |
          cd ontrack
          npm install
          cd ../backend
          npm install
      - name: testing
        run: |
          cd ../backend
          npm test
